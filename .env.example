# ==============================================================================
# Environment Configuration Template
# ==============================================================================
# This file serves as a template for required environment variables.
# Copy this file to .env and fill in your actual values.
#
# Purpose: Stores sensitive configuration values for the application
# such as database credentials, API keys, and service tokens.
# DO NOT commit this file to version control - use .env.example instead.
# ==============================================================================

# Backend Server Configuration
# --------------------------------------------------------------------------------
# Port number for the Express server to listen on
PORT=5000

# MongoDB Database Connection URI
# Format: mongodb+srv://<username>:<password>@<cluster>/<database>?retryWrites=true&w=majority
# Replace username, password, and cluster with your MongoDB Atlas credentials
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/eventsphere?retryWrites=true&w=majority

# JWT (JSON Web Token) Secret Key
# Used for signing and verifying authentication tokens
# Generate a strong random string for production
JWT_SECRET=your_jwt_secret_key_here

# Frontend Application URL
# Used for CORS configuration and redirects
FRONTEND_URL=http://localhost:3000

# Google OAuth 2.0 Configuration
# --------------------------------------------------------------------------------
# OAuth Client ID from Google Cloud Console
# Required for Google login functionality
GOOGLE_CLIENT_ID=your_google_client_id

# OAuth Client Secret from Google Cloud Console
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Email Service Configuration
# --------------------------------------------------------------------------------
# Gmail address used for sending transactional emails
# Must be a Gmail account with App Password enabled
EMAIL_USER=your_email@gmail.com

# Gmail App Password (not your regular Gmail password)
# Generate from: Google Account > Security > App Passwords
EMAIL_PASS=your_app_password

# Frontend Build Configuration
# --------------------------------------------------------------------------------
# API base URL for the React frontend to connect to
# This is used by axios/fetch requests in the frontend
REACT_APP_API_URL=http://localhost:5000/api
